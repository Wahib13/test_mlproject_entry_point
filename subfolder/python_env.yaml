# python_env.yaml
python: "3.12"

# Build‑time dependencies (optional; defaults include pip, setuptools, wheel)
build_dependencies:
  - virtualenv
  - pip
  - setuptools
  - wheel==0.37.1

# Runtime dependencies
dependencies:
  - mlflow==3.1.1
  - scikit-learn==1.7.0
